# Array
# Acessor pelo indice
# append()/pop() no fim
# Leitura O(1) Inserção O(n) Exclusão O(n)
# Move TODOS os elementos
# Perfeito para leitura

# Lista Encadeada
# Acesso sequencial
# Inserção e remoção em qualquer posição
# Leitura O(n) Inserção O(1) Exclusão O(1)
# Move apenas o próximo elemento
# Perfeito para inserções e remoções frequentes